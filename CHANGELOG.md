# Changelog

## Unreleased

- JSON 报告完整转义控制字符，确保输出可被标准 JSON 解析器读取。
- CLI 新增 `--redacted-only`，供下游程序直接消费净化文本。
- 新增 `redact_documents`，支持保留文档路径和顺序的批量脱敏。
- CI 增加 JSON 解析和敏感值不泄漏的冒烟检查；自动化测试增至 233 项。

## 0.1.1 - 2026-09-23

- 完成可交互 MVP：支持通过 `--text` 输入待扫描内容。
- 增加人类可读报告与完整 JSON 报告。
- 增加 `--help`、安全演示数据和可复现的 MVP 验收文档。
- 自动化测试增加至 231 项。

## 0.1.0 - 2026-09-20

- 首次公开版本。
- 增加流式扫描与默认脱敏策略。
- 增加令牌、配置键、PEM、连接串和结构化数据检测器。
- 增加自定义规则、白名单、位置映射和稳定指纹。
- 增加多文档扫描、文本、JSON 与 SARIF 报告。
- 增加示例、CI 和 229 项自动化测试。

