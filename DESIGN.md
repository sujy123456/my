# LunaSieve Design

LunaSieve is a streaming secret detection and redaction engine implemented in MoonBit.

The implementation is divided into independent packages for matching, detectors,
policy evaluation, structured data handling, reporting, and command-line use.

